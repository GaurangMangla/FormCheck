services:
  # FastAPI Backend Web Service (Docker container with OpenCV & MediaPipe)
  - type: web
    name: fitness-form-checker-backend
    env: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
